div(data-ng-controller="GiftController")
	div.box.text-center
		h1 My Gifts
		div.row.fades(ng-if="!auth")
			a(href='')
				button.btn.btn-promotion(ng-click="login()" style="" type="button") Log in to view your gifts
					br
					img(src="/img/facebook_login_button.png" style="height:5vh;width:auth;margin-top:10px")
			p.fb-warning
				| Taste will 
				b NEVER 
				| post anything without your explicit permission				
		div.fades(ng-if="gifts.length === 0")
			button.btn.btn-promotion sadly, you don't have any gifts right now
		div.fades(ng-repeat="gift in gifts")
			div.gift-box
				div.row
					div.col-xs-8.col-lg-8.col-xs-offset-2.col-lg-offset-2.text-center
						div.row
							div.col-xs-12.col-lg-12.text-center
								h2.text-center {{gift.store_name}}
													//- div.alert.alert-warning.alert-dismissible.text-center(ng-if="gift.alertMessage" role="alert")
								//- 	button.close(type="button" data-dismiss="alert")
								//- 		span(aria-hidden="true") &times;
								//- 		span.sr-only Close
								//- 		{{gift.alertMessage}}
								h1.text-center(style="white-space: normal") {{gift.display_text}}
								p.gift-text
									Valid from {{convertDate(gift.start_date)}} to {{convertDate(gift.end_date)}}

									//- /{{(new Date(gift.start_date)).getDate()}}/{{(new Date(gift.start_date)).getYear()}}  to {{(new Date(gift.end_date)).getMonth()}}/{{(new Date(gift.end_date)).getDate()}}/{{(new Date(gift.end_date)).getYear()}}
						div.row
							div.col-xs-6.col-xs-offset-3
								a(href="")
									img.img-responsive.gift-image(ng-click="redeemPromotion($index)" ng-src="./img/redeem_button.png")


